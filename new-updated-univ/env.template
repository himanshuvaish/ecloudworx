# Production Environment Variables
# Copy this to .env.production in your project root

# API endpoint (relative URL for reverse proxy)
VITE_STRAPI_URL=/api

# Node environment
NODE_ENV=production

# Port for development (not used in production Docker)
PORT=4000

# Optional: Add any other environment variables your app needs
# VITE_APP_NAME=eCloudWorx University
# VITE_VERSION=1.0.0
